apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: timefrontend
spec:
  hosts:
  - "timefrontend.default.svc.cluster.local"
  http:
  - route:
    - destination:
        host: timefrontend
        port:
          number: 80